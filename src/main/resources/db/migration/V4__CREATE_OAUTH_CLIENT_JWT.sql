CREATE TABLE client_oauth(id UUID PRIMARY KEY, client_id VARCHAR(50) NOT NULL, client_username VARCHAR(50) NOT NULL,
 client_secret VARCHAR(100) NOT NULL, redirect_uri VARCHAR(100) NOT NULL, client_scope VARCHAR(50) NOT NULL);